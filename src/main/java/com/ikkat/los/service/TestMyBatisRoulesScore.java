package com.ikkat.los.service;

//import org.apache.ibatis.annotations.Mapper;
//import org.apache.ibatis.annotations.Select;

import com.ikkat.los.helper.entity.ParamsRoulesScoreEntity;

//@Mapper
public interface TestMyBatisRoulesScore {
//	@Select("SELECT SUM(score) FROM roulesscore WHERE "
//			+ " (groups='age' and minvalue < #{valuenumberage} and #{valuenumberage} > minvalue and maxvalue >= #{valuenumberage}) ")
//	public double scoretestmybatis(ParamsRoulesScoreEntity params);
}
